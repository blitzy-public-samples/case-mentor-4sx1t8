# Required for authentication and database services
# Requirement: Authentication & Security - Secure configuration for Supabase JWT-based authentication
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API Configuration
# Required for backend API communication
# Requirement: System Security - Secure handling of sensitive configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Application Configuration
# Required for authentication callbacks and redirects
# Requirement: Authentication & Security - Secure configuration for authentication flows
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe Configuration
# Required for payment processing integration
# Requirement: External Services Integration - Configuration for Stripe payment processing
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-publishable-key