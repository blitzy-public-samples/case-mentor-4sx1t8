# @requirement: Security Architecture - Secure configuration management for API keys, tokens, and sensitive credentials
# @requirement: System Configuration - Configuration settings for core services

# Node Environment (development/staging/production)
NODE_ENV=development

# Supabase Configuration
# Required: Database and authentication configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Database Pool Configuration
# Optional: Connection pool size limits (defaults shown)
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# OpenAI Configuration
# Required: API key for AI evaluation services
OPENAI_API_KEY=sk-your-openai-api-key
# Optional: Model and token settings (defaults shown)
OPENAI_MODEL=gpt-4
OPENAI_MAX_TOKENS=2048

# Stripe Configuration
# Required: Payment processing credentials
STRIPE_SECRET_KEY=sk_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Email Configuration (Resend)
# Required: Email service credentials
RESEND_API_KEY=re_your-resend-api-key
# Optional: Default sender address (default shown)
EMAIL_FROM_ADDRESS=noreply@caseinterviewpractice.com